fastapi==0.136.3
# Pinned explicitly: fixes CVE-2025-54121 (>=0.47.2) and CVE-2025-62727 (>=0.49.1).
starlette==1.2.1
uvicorn[standard]==0.34.2
websockets==15.0.1
aiosqlite==0.21.0
# Migrated off python-jose (unmaintained; hard-pinned vulnerable pyasn1<0.5,
# CVE-2026-30922). PyJWT pulls no pyasn1/rsa/ecdsa; we sign with HS256 (stdlib HMAC).
PyJWT==2.13.0
bcrypt==4.3.0
pyotp==2.9.0
# Bumped 1.1.0 -> 1.2.2 for CVE-2026-28684.
python-dotenv==1.2.2
pydantic==2.11.1
pydantic-settings==2.8.1
docker==7.1.0
aiosmtplib==3.0.2
email-validator==2.2.0
# Upper bound added (DEPS-02) to prevent unpinned major-version drift on rebuilds.
cryptography>=44.0.0,<49.0.0
