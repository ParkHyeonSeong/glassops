psutil==7.0.0
websockets==15.0.1
# Bumped 1.1.0 -> 1.2.2 for CVE-2026-28684 (kept in lockstep with the backend pin;
# the unified image installs both requirement sets together).
python-dotenv==1.2.2
docker==7.1.0
pynvml==12.0.0
